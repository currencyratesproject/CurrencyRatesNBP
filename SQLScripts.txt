Z racji tego ¿e do tworzenia table w bazie danych zastosowa³em podejœcie Code First, zosta³y one wygenerowane automatycznie. Natomiast jeœli skorzyst¹³ bym np. z podejœcia Model First,
musia³bym stworzyæ nastêpuj¹ce dwie tabele w bazie. 

CREATE TABLE POSITIONS(
ID INT NOT NULL PRIMARY KEY,
DATA DATE,
CURRENCYNAME VARCHAR(50),
CURRENCYCODE VARCHAR(5),
CURRENCYCONVERSION INT,
AVERAGE FLOAT

);

CREATE TABLE DIRS(
ID INT NOT NULL PRIMARY KEY,
DIRNUMBER VARCHAR(50)
);

Wszystkie operacje wyjmowania danych z bazy i zapisu danych do niej w kodzie odbywaj¹ siê za pomoc¹ jêzyka LINQ. Podaje przyk³ady takich operacji w jêzyku t-sql:

--Dodawanie rekordów do tabeli POSITIONS
INSERT INTO POSITIONS (DATE,CURRENCYNAME,CURRENCYCODE,CURRENCYCONVERSION,AVERAGE) VALUES ('01-02-2010','DOLAR AMERYKANSKI','USD',1,3.43);

--Wyci¹ganie z tabeli POSITIONS unikalnych nazw walut i sorowanie ich wed³ug porz¹dku alfabetycznego
SELECT DISTINCT CURRENCYNAME FROM POSITIONS ORDER BY CURRENCYNAME;

--Srednia cena waluty ze wszystkich danych 
SELECT AVG(AVERAGE) FROM POSITIONS;

--Maxymalna cena waluty ze wszystkich danych (analogicznie minimalna -> funkcja MIN() )
SELECT MAX(AVERAGE) FROM POSITIONS;

--Sprawdzenie iloœci pozycji w tabeli POSITIONS
SELECT COUNT(*) FROM POSITIONS;

--Wyci¹ganiêcie z bazy rekrdów których CURRENCYCOD == 'code'
SELECT * FROM POSITIONS WHERE CURRENCYCODE = 'USD'

